{% extends 'base.html.twig' %}

{% block title %}equipe!{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.poste) }}
    {{ form_row(form.image) }}
    <input type="submit" value="Editer" class="btn btn-secondary">
    {{ form_end(form) }}
{% endblock %}